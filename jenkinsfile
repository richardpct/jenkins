pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls -la'
            }
        }
        stage('Test') {
            steps {
                echo 'ls -la'
            }
        }
        stage('Deploy') {
            steps {
                echo 'ls -la'
            }
        }
    }
}
