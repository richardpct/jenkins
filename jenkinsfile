pipeline {
  agent any
  stages {
    stage('Build') { steps { sh 'echo build && sleep 1' } }
    stage('Test')  { steps { sh 'echo test && sleep 1' } }
    stage('Deploy'){ steps { sh 'echo deploy && sleep 1' } }
  }
}
